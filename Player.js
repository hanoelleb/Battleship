
const Player = (pid) => {

    var id = pid;
    var board = Gameboard(id);

    const placeShip(gb, row, col, id) => {
        gb.place(row,col,id);
    }

    const makeMove = (enemyGb, row, col) => {
    }
}
